#tane the results from completeness.txt (generated by completeness.py)
# scrubs all those accessions from the results
# they will be redone later

patterns = set()
for line in open("check_results.completeness.txt"):
    patterns.add(line.strip())

import sys
for line in sys.stdin:
    if line[0] == '>':
        acc = line[1:].split("_")[0]
        skip = acc in patterns
        if not skip:
            print(line, end="")
    else:
        if not skip:
            print(line, end="")
        skip = False
